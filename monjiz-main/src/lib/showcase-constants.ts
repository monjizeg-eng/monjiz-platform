/** Public profile shows up to this many showcase projects, each with its own image collage. */
export const MAX_SHOWCASE_PROJECTS = 4;

/** Max images allowed in one project's collage (dashboard + admin editor). */
export const MAX_IMAGES_PER_PROJECT_COLLAGE = 10;
